# Mac inference + app environment (Apple Silicon, MPS)
# Do NOT add fairseq, pyaudio, or portaudio here -- mic capture happens in the
# browser via the Web Audio API, and HF transformers replaces fairseq.
torch              # official wheels support MPS on Apple Silicon
torchaudio
transformers
librosa
soundfile
scikit-learn
numpy
pandas             # src/extract_embeddings.py, src/eval_eer_embeddings.py
pyarrow            # parquet read/write for the above
fastapi
uvicorn
websockets
python-multipart
requests           # sdk/voiceguard_client.py integration snippet
